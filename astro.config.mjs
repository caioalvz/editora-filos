import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Atualize com o domínio final
  site: 'https://filoseditora.com.br',
  compressHTML: true,
});
